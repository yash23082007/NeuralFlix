class DiversityService:
    @staticmethod
    def ensure_diversity(recommendations: list, max_same_genre: int = 3) -> list:
        # Dummy implementation for now
        return recommendations

diversity_service = DiversityService()
