# Movie intelligence evaluation

The repository currently ships a deterministic recommender. Offline model claims must be generated by:

```text
python -m pipeline.evaluation.run_eval
```

The command uses a checked-in fixture until MovieLens ingestion is configured. Its output is a smoke test for the evaluation contract, not a claim about NeuralFlix users or production quality.
