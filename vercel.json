{
  "framework": "nextjs",
  "buildCommand": "cd frontend-next && npm run build || npm run build",
  "outputDirectory": "frontend-next/.next",
  "installCommand": "cd frontend-next && npm install || npm install",
  "devCommand": "cd frontend-next && npm run dev || npm run dev",
  "regions": ["bom1"],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options",        "value": "DENY" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy",        "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
